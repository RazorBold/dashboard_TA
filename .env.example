# .env — Konfigurasi Flask Server (di VM)
# Salin jadi .env: cp .env.example .env

# MQTT — Flask subscribe dari broker di VM yang sama (pakai localhost)
MQTT_HOST=localhost
MQTT_PORT=14583
MQTT_ENABLED=true
